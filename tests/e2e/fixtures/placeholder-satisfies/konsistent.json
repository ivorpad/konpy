{
  "version": "v1",
  "conventions": [
    {
      "name": "ai-provider-stems",
      "paths": "packages/{providerId}",
      "must": [
        { "must": { "haveFiles": ["src/index.py"] } },
        {
          "if": { "placeholderSatisfies": "providerId:matches(^[a-z]+ai$)" },
          "must": { "haveFiles": ["src/${providerId}-stem.py"] }
        }
      ]
    }
  ]
}
