{
  "version": "v1",
  "conventions": [
    {
      "name": "ai-provider-stems",
      "paths": "packages/{providerId:matches(^[a-z]+ai$)}/src/${providerId}-stem.py",
      "must": {
        "exportConstants": ["${providerId.extract(^([a-z]+)ai$)}"]
      }
    }
  ]
}
